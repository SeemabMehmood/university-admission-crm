$("#add_email_template").modal();
$("#add_email_template").html("<%= escape_javascript(render 'email_templates/edit') %>");

$('[data-provider="summernote"]').each(function() {
  return $(this).summernote({
    height: 300
  });
});
